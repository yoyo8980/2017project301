<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="com.hb.major.model.checkMaster.CheckMasterDao">
 
  	
  	<select id="masterCountId" parameterType="String" resultType="int">
  		SELECT COUNT(*) FROM ADMININFO WHERE ADMIN_ID = #{value}
  	</select>

  	
  </mapper>